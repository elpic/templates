#!/bin/sh
# Generated from setup.bp — do not edit directly.
# Regenerate: blueprint render setup.bp --template . --output . --var APP_NAME={{ var "APP_NAME" }}
set -e

###########################
# Dependencies            #
###########################

# Always sync dependencies — npm cache makes this near-instant when nothing changed.
echo 'Installing dependencies'
npm install

###########################
# Pre-start hooks         #
###########################

{{ if ne (default "PRE_START_LOCAL" "") "" }}
echo 'Running pre-start hooks'
{{ default "PRE_START_LOCAL" "" }}
{{ end }}

###########################
# Start application       #
###########################

exec "$@"
