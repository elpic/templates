#!/bin/sh
# Generated from setup.bp — do not edit directly.
# Regenerate: blueprint render setup.bp --template entrypoint.sh.tmpl --var APP_NAME={{ var "APP_NAME" }} --output entrypoint.sh
set -e

# ── Pre-start hooks ───────────────────────────────────────────────────────────
# Add your pre-start commands here. Example: run database migrations.
#
# Django:
#   python -m {{ var "APP_NAME" }}.manage migrate --noinput
#
# Alembic:
#   python -m alembic upgrade head

# ── Start application ─────────────────────────────────────────────────────────
exec "$@"
