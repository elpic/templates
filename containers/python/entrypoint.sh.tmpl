#!/bin/sh
# Generated from setup.bp — do not edit directly.
# Regenerate: blueprint render setup.bp --template . --output . --var APP_NAME={{ var "APP_NAME" }}
set -e

###########################
# Pre-start hooks         #
###########################

# Configure pre-start commands via PRE_START variable in setup.bp or --var.
# Examples:
#   --var PRE_START="python manage.py migrate && python manage.py collectstatic --no-input"

{{ if ne (default "PRE_START" "") "" }}
echo 'Running pre-start hooks'
{{ default "PRE_START" "" }}
{{ end }}

###########################
# Start application       #
###########################

exec "$@"
